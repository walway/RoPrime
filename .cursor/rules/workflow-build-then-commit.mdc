---
description: Always build (bun) and commit after changes
alwaysApply: true
---

# Workflow: build then commit

- After making any code change, run `bun run build`.
- If the user asked to commit (or it’s the natural next step), commit the resulting changes after the build succeeds.
- If the build fails, fix the build first; do not commit a failing build.

